# XAUUSD trading bot — runtime dependencies
# Python 3.11+ required (see .python-version).

# ---- Broker / market data ----
# Note: MetaTrader5 is officially Windows-only. On the Hetzner Linux VPS
# (Phase G) we'll run MT5 + Python under Wine. Local dev should be on
# Windows, or macOS/Linux with MT5 + Python in a Windows VM.
MetaTrader5>=5.0.45

# ---- Data handling ----
pandas>=2.2,<3.0
# numpy<2 for MetaTrader5 ABI compat (numpy 2.x has breaking changes
# some downstream libs haven't caught up with).
numpy>=1.26,<2.0
# pandas-ta was speculatively added during Phase B but never imported —
# the S&D strategy modules (PR #31) use raw pandas/numpy, not an
# indicator library. Removed because pandas-ta 0.4.x bumped its numpy
# floor to >=2.2.6, which conflicts with our numpy<2.0 pin and broke
# fresh-env installs (reported against Python 3.12.7). Add back only
# when an indicator is genuinely needed.

# ---- Database ----
supabase>=2.3

# ---- Config / env ----
python-dotenv>=1.0
pydantic>=2.5
pydantic-settings>=2.1

# ---- HTTP (Telegram alerts, ad-hoc; bot itself doesn't call Finnhub) ----
requests>=2.31

# ---- Logging ----
loguru>=0.7

# ---- Backtest visualisation (Phase D) ----
# Plotly is the only chart dep — works in Colab, browser, and exports
# to standalone HTML with no JS deps when embedded.
plotly>=5.20

# ---- Dev / test ----
pytest>=7.4
pytest-mock>=3.12
ruff>=0.1
black>=23.12
# nbformat: validates the Colab demo notebook structure in tests
# (test_backtest_demo_notebook.py). Not needed at bot runtime.
nbformat>=5.10

# Deferred — added in their respective phases:
#   python-telegram-bot          Phase E (dashboard / alerts)
