"""Tests for ``bot.data.ohlc_provider``.

Real tests (cache hit/miss timing, invalidate behaviour, DataFrame
isolation) land in Phase B alongside the strategy modules that
consume this provider. Placeholder for now.
"""
